package org.stealthguard.gateway;

import org.junit.jupiter.api.Test;

/** The whole gateway wires up against a real Postgres (Flyway-migrated). */
class StealthguardGatewayApplicationTests extends AbstractPostgresTest {

    @Test
    void contextLoads() {
    }
}